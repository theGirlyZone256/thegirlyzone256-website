{
  "name": "thegirlyzone-uganda",
  "version": "1.0.0",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "dev": "eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@netlify/plugin-sitemap": "^0.8.1"
  }
}
